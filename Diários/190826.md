# Rascunho de Relatório — ProcessFlow (sessão 19/08)

> Preencher datas/horários exatos consultando `evidencias.log`. Os itens abaixo estão em ordem cronológica da sessão de hoje, mas os timestamps precisos precisam ser copiados do log (comando `less evidencias.log` ou `grep` pela hora).

## 5.1 Estratégias da semana

| Estratégia | Nome curto | Contexto | Motivo da troca (se houve) |
|---|---|---|---|
| S1 | Parser + dispatcher em array fixo | Tokenização de linha com `strtok`, comandos despachados via `strcmp` em cadeia `if/else if`. Decidido usar array fixo (`char *args[64]`) em vez de lista encadeada para tokens de linha, pois `execvp` exige array contíguo terminado em NULL. | — |
| S2 | Módulos separados (task.h/task.c, exec.h/exec.c) | Ao invés de concentrar tudo em `main.c`, separou responsabilidades: `task.c` cuida de dados (cadastro/busca de tarefas), `exec.c` cuida de execução de processos (fork/exec/wait). | — |

## 5.2 Diário de Tentativas

| # | Estrat. | O que tentei | Resultado | Hipótese/Causa (se falhou) | Quando | Evidência |
|---|---|---|---|---|---|---|
| 1 | S1 | Loop interativo com `fgets` + prompt `processflow>` | OK | — | (ver evidencias.log) | evidencias.log |
| 2 | S1 | Tokenização de linha com `strtok(buf, " ")`, guardando ponteiros em `args[64]` | Falha parcial → corrigida | Primeira versão tentou `strcpy(args[i], token)` — `args[i]` era ponteiro não inicializado, causaria escrita em memória inválida. Corrigido para apenas atribuir o ponteiro (`args[i] = token`), já que `token` aponta para dentro de `buf`. | (ver evidencias.log) | evidencias.log |
| 3 | S2 | Struct `Task` em `task.h` com array de argumentos `char *argumentos[64]` | Falha parcial → corrigida | Primeira tentativa usou array de tamanho flexível (`char *argumentos[]`) dentro da struct, que não funciona sem alocação manual especial. Trocado para array fixo de tamanho 64. | (ver evidencias.log) | evidencias.log |
| 4 | S2 | `criar_task()` copiando nome/programa/argumentos para a struct | Falha parcial → corrigida | Bug 1: `strcpy` direto em ponteiros não alocados dentro de `argumentos[]` — trocado por `strdup` (aloca memória nova). Bug 2: `tasks->qtdArgumentos = ...` escrevia sempre no índice 0 em vez de `tasks[contador_tasks]`, por `tasks` ser array e `->` equivaler a `tasks[0]`. Corrigido para indexação explícita. | (ver evidencias.log) | evidencias.log |
| 5 | S2 | `encontrar_task()` buscando por nome | Falha parcial → corrigida | `strcmp(nome, tasks[i])` comparava string com struct inteira (tipo incompatível); `return tasks[i]` retornava struct por valor em função que devia retornar `Task*`. Corrigido para `tasks[i].nome` e `&tasks[i]`. | (ver evidencias.log) | evidencias.log |
| 6 | S2 | `spawn()`: fork + execvp + waitpid em `exec.c` | Falha parcial → corrigida | Faltava `exit(1)` no filho após falha de `execvp` — sem isso, o processo filho continuaria executando o código do pai (loop interativo duplicado). Corrigido adicionando `exit(1)` no branch de erro. | (ver evidencias.log) | evidencias.log |
| 7 | S1/S2 | Teste ponta a ponta: `task listar /bin/ls -l` + `run sequential listar` | OK | — | (ver evidencias.log) | evidencias.log |
| 8 | S2 | Testes de erro: tarefa inexistente, programa inexistente, argumentos insuficientes | OK | — | (ver evidencias.log) | evidencias.log |
| 9 | S2 | Makefile (`all`/`clean`, `-Wall -Wextra`) | OK | — | (ver evidencias.log) | evidencias.log |
| 10 | S1 | `run sequential` generalizado para N tarefas (loop) | Falha parcial → corrigida | Bug: dentro do loop usava `args[2]` fixo em vez de `args[i]`, executando sempre a mesma tarefa. Corrigido. | (ver evidencias.log) | evidencias.log |
| 11 | S2 | `run parallel`: nova função `spawn_async()` sem `waitpid` interno, criando todos os filhos antes de esperar qualquer um | Falha parcial → corrigida | Bug 1: `return;` em função `pid_t` (sem valor) no caminho de erro do fork — corrigido para `return -1;`. Bug 2: loop de `waitpid` no `main.c` chamava `waitpid(pids[i], ...)` mesmo quando `pids[i] == -1`, o que equivale a "esperar qualquer filho" (`waitpid(-1,...)`) e rouba/bagunça a espera de outros filhos legítimos. Corrigido filtrando `if (pids[i] > 0)`. | (ver evidencias.log) | evidencias.log |
| 12 | S2 | Teste de paralelismo real: `task a sleep 2` / `task b sleep 1` / `run parallel a b` | OK — tempo total ~2s, confirma execução concorrente | — | (ver evidencias.log) | evidencias.log |

## Notas para as próximas seções do relatório

- **Decisões técnicas** (seção 4 do guia) já dá pra puxar direto daqui: array fixo vs. lista encadeada, separação em módulos, `spawn` vs `spawn_async`.
- **Reflexão final**: mencionar o padrão de bugs recorrente (ponteiros não inicializados / tipos incompatíveis entre struct e string) como aprendizado.
- Falta preencher **seção 7 (Uso de IA)** — usei assistente como mentor/revisor de código (não escreveu a lógica principal do programa; apontou bugs, explicou conceitos de C como `strtok`, ponteiros, `fork`/`exec`/`wait`, e ajudou a montar o Makefile). Validação manual: cada trecho foi testado rodando `./processflow` com casos reais antes de prosseguir.
